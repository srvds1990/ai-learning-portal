import React from "react";

const LLM = () => {
  return (
    <div>
      <p>
        Large Language Models (LLMs) are advanced deep learning models trained
        on massive datasets to understand and generate human-like text.
      </p>
      <ul>
        <li>Pre-trained models like GPT, BERT, LLaMA</li>
        <li>Applications in chatbots, summarization, translation</li>
        <li>Zero-shot and few-shot learning capabilities</li>
      </ul>
      <strong>Example:</strong> GPT-4 writing an essay or summarizing research papers.
    </div>
  );
};

export default LLM;