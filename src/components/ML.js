import React from "react";

const ML = () => {
  return (
    <div>
      <p>
        Machine Learning (ML) is a subset of AI where algorithms learn from
        historical data to make predictions or decisions.
      </p>
      <ul>
        <li>Supervised Learning (e.g., classification, regression)</li>
        <li>Unsupervised Learning (e.g., clustering, anomaly detection)</li>
        <li>Reinforcement Learning (e.g., game-playing AI)</li>
      </ul>
      <strong>Example:</strong> Netflix recommending movies based on your watch history.
    </div>
  );
};

export default ML;