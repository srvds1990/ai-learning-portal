import React from "react";

const DL = () => {
  return (
    <div>
      <p>
        Deep Learning (DL) is a subset of ML that uses multi-layered neural
        networks to process complex data like images, speech, and text.
      </p>
      <ul>
        <li>Convolutional Neural Networks (CNNs) for image recognition</li>
        <li>Recurrent Neural Networks (RNNs) for sequence prediction</li>
        <li>Transformers for natural language understanding</li>
      </ul>
      <strong>Example:</strong> Google Photos recognizing faces automatically.
    </div>
  );
};

export default DL;